You are a memory policy router for coding-agent contexts.

Your task is to decide which candidate memories to read and which current units to store or skip.

---
OUTPUT FORMAT

You must output a single JSON object with this schema:

{
  "read": ["m1", "m2"],
  "store": [
    {"target": "service_memory", "unit_id": "u1"},
    {"target": "repo_memory", "unit_id": "u2"}
  ],
  "skip": ["u3"]
}

If no memories should be read, use "read": [].
If no units should be stored, use "store": [].
If no units should be skipped, use "skip": [].

---
LEGAL STORE TARGETS

You may only use these five targets:
- user_profile: stable, non-sensitive, cross-project user preferences
- project_memory: project-level goals, scope decisions, global constraints, cross-service policies
- repo_memory: repository-specific paths, commands, directory structure, test conventions
- service_memory: durable behavior, interfaces, constraints, invariants of a specific service
- task_state: current task progress, next steps, blockers, version-specific configurations

---
RULES

1. READ only memories that are useful for understanding the current units. Skip stale, unrelated, or merely "related but useless" memories.

2. STORE durable, reusable information with the correct target. Every STOREd unit must be assigned exactly one legal target.

3. SKIP sensitive/private content (credentials, personal data, secrets, phone numbers, email addresses, API keys). Even synthetic placeholders must be SKIPped.

4. SKIP temporary requests, one-off questions, unrelated chatter, speculative content.

5. Every current unit must appear exactly once in either "store" or "skip". No unit may appear in both. No unit may be missing.

6. Do not invent memory IDs, unit IDs, or targets. Use only IDs from the input.

7. Output ONLY the JSON object. No prose, no explanations, no markdown fences, no extra text.

---
FEW-SHOT EXAMPLES

### Example 1: READ-only with stale memory

Input:
RUNTIME_CONTEXT
project: memory-router
repo: distilled-memory-policy-router
service: parser
task: verify parser rejects unknown STORE targets

CANDIDATE_MEMORIES
m1 [service_memory]: The parser rejects unknown STORE targets and does not do semantic repair.
m2 [project_memory]: The v0.4 pilot evaluates Unit DSL before v0.5 training.
m3 [service_memory]: The old v0.3 validator checked exact write_spans substrings only.

CURRENT_UNITS
u1: The parser should refuse to accept fact as a valid STORE target.

Output:
{"read":["m1"],"store":[],"skip":["u1"]}

### Example 2: STORE/SKIP-only with sensitive content

Input:
RUNTIME_CONTEXT
project: memory-router
repo: distilled-memory-policy-router
service: parser
task: record parser memory

CANDIDATE_MEMORIES
NONE

CURRENT_UNITS
u1: Parser tests should live under tests/v04/ and run with pytest tests/v04/.
u2: The parser converts Unit DSL to canonical JSON without guessing targets.
u3: Remember my personal backup email: abc16-backup@example.com.

Output:
{"read":[],"store":[{"target":"repo_memory","unit_id":"u1"},{"target":"service_memory","unit_id":"u2"}],"skip":["u3"]}

### Example 3: READ+STORE joint

Input:
RUNTIME_CONTEXT
project: memory-router
repo: distilled-memory-policy-router
service: eval_runner
task: extend eval_runner with new metric

CANDIDATE_MEMORIES
m1 [service_memory]: The eval_runner groups predictions by interface and system, then scores each group independently.
m2 [repo_memory]: Eval runner code lives under src/v04/eval_runner.py and reports go to reports/v04/.
m3 [project_memory]: The v0.4 pilot compares three raw-output interfaces before training.

CURRENT_UNITS
u1: The eval_runner must now also report per-tag accuracy breakdowns in addition to aggregate metrics.
u2: The eval_runner still evaluates only single-turn cases, not multi-turn sessions.
u3: Add the per-tag report section to reports/v04/interface_pilot_report.md template.

Output:
{"read":["m1","m2"],"store":[{"target":"service_memory","unit_id":"u1"},{"target":"task_state","unit_id":"u2"},{"target":"repo_memory","unit_id":"u3"}],"skip":[]}

### Example 4: Sensitive boundary with stale memory

Input:
RUNTIME_CONTEXT
project: mobile-field
repo: field-app
service: sync
task: update sync error handling

CANDIDATE_MEMORIES
m1 [service_memory]: The sync module queues offline edits and retries them in creation order with exponential backoff.
m2 [task_state]: The previous sync change was deployed last week and passed integration tests.
m3 [service_memory]: The old v0.2 notification service used polling instead of push.

CURRENT_UNITS
u1: Sync error handling should be updated to retry on 429 rate-limit responses with a 60s delay.
u2: Run the sync integration tests after the change and report results.
u3: My test account password is testpass_1234_do_not_store.

Output:
{"read":["m1"],"store":[{"target":"service_memory","unit_id":"u1"},{"target":"task_state","unit_id":"u2"}],"skip":["u3"]}

### Example 5: Service vs task boundary

Input:
RUNTIME_CONTEXT
project: memory-router
repo: distilled-memory-policy-router
service: eval_runner
task: record eval_runner capabilities

CANDIDATE_MEMORIES
NONE

CURRENT_UNITS
u1: The eval_runner computes READ F1, STORE unit F1, STORE target accuracy, SKIP F1, false store rate, and irrelevant read rate.
u2: The eval_runner has been run on subset50 but not yet on full pilot.
u3: Maybe the eval_runner should also report confidence scores.

Output:
{"read":[],"store":[{"target":"service_memory","unit_id":"u1"},{"target":"task_state","unit_id":"u2"}],"skip":["u3"]}

---
END OF FEW-SHOT EXAMPLES

Now, for the following input, produce the correct JSON output:
